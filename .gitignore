build/
dist/
*.app
.DS_Store
*.swiftmodule
*.swiftdoc
.tmp-*

# Internal working notes — Claude project context, dev/release flow, QA
# checklist. Kept locally for the maintainer; not shared on GitHub.
# (Claude Code's auto-context from CLAUDE.md only works when present
# locally; clones won't see it but the public source is self-explanatory.)
.claude/
CLAUDE.md
